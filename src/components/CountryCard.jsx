function CountryCard({foundCountries}){
    return( <>
        <section>
            <article className="country-card">
                <p> vlag </p>
                <h4>{props.name}</h4>
                <p>Has a population of {props.population} people</p>
            </article>
        </section>

    </>);
}